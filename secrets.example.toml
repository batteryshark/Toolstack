# Dev secret backend for the toolyard FileBackend (Phase 2).
# Copy to secrets.toml (gitignored) and fill in real values. Shape: [<tool_id>].
# Production uses SOPS or Infisical behind the same resolve() interface.

[echo]
API_KEY = "dev-secret-123"
