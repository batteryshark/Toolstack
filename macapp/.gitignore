# SwiftPM build products
.build/
# Packaged app bundle / notarization artifacts (produced by scripts/build-app.sh)
build/
*.app
*.dmg
*.zip
# Local signing config (Developer ID identity / team id / notary profile name)
packaging/signing.env
